<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Workspace code fixes for the Cntryl.Conventions.Testing.Analyzers diagnostics.</Description>
    <PackageId>Cntryl.Conventions.Testing.CodeFixes</PackageId>
    <AssemblyName>Cntryl.Conventions.Testing.CodeFixes</AssemblyName>
    <RootNamespace>Cntryl.Conventions.Testing.CodeFixes</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Pinned to the minimum supported Roslyn so the code fixes cannot use a newer API. -->
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.Workspaces" VersionOverride="5.0.0" PrivateAssets="all"/>
    <ProjectReference Include="../Conventions.Testing.Analyzers/Conventions.Testing.Analyzers.csproj"/>
  </ItemGroup>

</Project>
